{
  "$schema": "https://v2-8-13.turborepo.dev/schema.json",
  "globalEnv": ["CI", "MINERVA_DISABLE_SHARE"],
  "globalPassThroughEnv": ["CI", "MINERVA_DISABLE_SHARE"],
  "tasks": {
    "typecheck": {},
    "build": {
      "dependsOn": [],
      "outputs": ["dist/**"]
    },
    "opencode#test": {
      "dependsOn": ["^build"],
      "outputs": [],
      "passThroughEnv": ["*"]
    },
    "@minerva-ai/app#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@minerva-ai/ui#test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
